{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildContext": "laravel-app"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan migrate:fresh --force && php artisan serve --host=0.0.0.0 --port=$PORT --env=production"
  }
}